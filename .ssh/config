# This ssh config should be pasted into the ~/.ssh/config file.
# Using this config, you can ssh into a lab machine using `ssh ic`
# Of course you should replace `abc17` with your username, and `voxel02` with your fav machine
 
Host broker
  User js6317
  Hostname shell2.doc.ic.ac.uk 
  LocalForward 2222 gpu12:22
  # Port forwarding for Tensorboard.
  LocalForward 16006 127.0.0.1:16006

Host gpu12
  User js6317
  Hostname gpu12.doc.ic.ac.uk
  ProxyJump broker
  ForwardX11 yes
  LocalForward 16006 127.0.0.1:6006
  # ProxyCommand ssh -q -X broker nc -q0 gpu12.doc.ic.ac.uk 22

